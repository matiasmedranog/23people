apiVersion: v1
kind: Service
metadata:
  name: {{ include "23people.fullname" . }}
  labels:
    {{- include "23people.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "23people.selectorLabels" . | nindent 4 }}
