apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "23people.fullname" . }}-test-connection"
  labels:
    {{- include "23people.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "23people.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
